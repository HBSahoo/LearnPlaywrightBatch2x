function getUserStatus() { // JS Engine
    //var status_code; Executed by JS Engine & (optimized the code)
    console.log(status_code);
    var status_code = "Active";
    console.log(status_code);
}

getUserStatus();
// Note: var is function-Scoped, so ststus is hoisted to the top of getUserStatus(), Not the Global scope.
